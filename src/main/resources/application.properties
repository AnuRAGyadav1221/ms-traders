# === App Config ===
spring.application.name=Shopping_Application

# === RDS Database Config ===
spring.datasource.url=jdbc:mysql://mstraders-db.c3u0asie0e59.ap-south-1.rds.amazonaws.com:3306/shopping_application_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
# spring.datasource.password=root1234
spring.datasource.password=${DB_PASSWORD}

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.profiles.active=dev
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# === Thymeleaf ===
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML

# === Static Resources & Uploads ===
# spring.web.resources.static-locations=classpath:/static/
# file.upload-dir=/tmp/uploads

# === Error Handling ===
server.error.include-message=always
server.error.include-binding-errors=always

# === View Resolver ===
# spring.mvc.view.prefix=/WEB-INF/views/
# spring.mvc.view.suffix=.jsp

# === Multipart Config ===
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=15MB

# === Email (SMTP) ===
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=anuragsinghy001@gmail.com
# spring.mail.password=tjaxttascmkbycin
spring.mail.password=${MAIL_PASSWORD}

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# === Razorpay ===
# razorpay.key=rzp_test_4YsEOtg6LlSYKR
# razorpay.secret=mni3rBT5wyTyQGjANWW393pk

razorpay.key=${RAZORPAY_KEY}
razorpay.secret=${RAZORPAY_SECRET}

# === AWS S3 ===
cloud.aws.credentials.access-key=${AWS_ACCESS_KEY_ID}
cloud.aws.credentials.secret-key=${AWS_SECRET_ACCESS_KEY}

cloud.aws.region.static=ap-south-1

application.s3.bucket.category=mstrader-category
application.s3.bucket.product=mstrader-product
application.s3.bucket.profile=mstrader-profile
application.s3.bucket.banner=mstrader-banner

# === Server Port ===
server.port=${PORT:8080}
